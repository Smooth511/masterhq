Common Ethernet Naming Conventions 
enp... or eno...: Predictable interface names (e.g., enp0s31f6 or enp0s3).
eth0 / eth1: Traditional naming style. 
Ubuntu
Ubuntu
 +2
Troubleshooting
If your ethernet isn't working:
Verify the hardware is detected: lspci | grep -i ethernet.
Check for active connections: nmcli dev.
Check if the cable is plugged in, as "cable unplugged" errors often show in dmesg -w. 
Reddit
Reddit
 +2
AI responses may include mistakes. Learn more




Configuring networks - Ubuntu Server documentation

Ubuntu
Ubuntu Linux Display List of Ethernet Adapter - nixCraft

nixCraft

Ubuntu not registering my Ethernet connection - Reddit

Reddit

Show all

undefined
undefined
undefined
14 sites
In Linux, kcore refers to a virtual file that represents the physical memory (RAM) of your system. You can find it at: 
/proc/kcore 
Key Facts About /proc/kcore
Virtual Nature: It is part of the proc virtual filesystem and does not occupy any actual disk space.
Massive Size: On 64-bit systems, it often displays a size of 128 TiB (around 140 TB). This is a map of the entire addressable virtual memory space, not the amount of RAM actually installed.
Usage: It is primarily used by debuggers like GDB to examine the current state of kernel data structures in real-time.
Safety: You cannot delete or modify this file; it is a "window" into your system's memory provided by the kernel. 
How to View It
You can see its reported size by running the following command in your terminal:
ls -lh /proc/kcore 
Ask Ubuntu
Ask Ubuntu
 +1
Note: If you were looking for k-core in the context of graph theory or network analysis, it refers to a maximal subgraph where every node has at least degree kk
𝑘
. This is commonly used in social network analysis and can be calculated using libraries like NetworkX. 
AI responses may include mistakes. Learn more




Absurdly large /proc/kcore file -- what does it mean? - Ask Ubuntu

Ask Ubuntu

What type of file is /dev/core or /proc/kcore?

Unix & Linux Stack Exchange
Ubuntu Manpage: /proc/kcore - physical memory

Ubuntu Manpage
Show all

undefined
undefined
undefined
12 sites
To fix an Ethernet connection that won't connect in Ubuntu, start by checking if the system recognizes your hardware and then verify the software configuration. 
1. Check Physical and Hardware Status 
Before running terminal commands, perform these quick physical checks:
Check the Port Lights: Look at the Ethernet port on your computer and router. A solid or blinking light usually indicates a physical link.
Verify Hardware Detection: Run lspci | grep -i ethernet in your terminal.
If nothing appears: Your network card may be disabled in BIOS or is physically faulty.
If it appears: Ubuntu sees the card, but the driver or configuration might be the issue. 
Ubuntu Community Hub
Ubuntu Community Hub
 +6
2. Identify the Interface Status
Use the following commands to see how Ubuntu sees your Ethernet device: 
ip link show: Lists all network interfaces. Look for names like enp... or eth0.
UP: The interface is active.
NO-CARRIER: The cable is likely unplugged or the port is dead.
nmcli device status: Shows if the device is "connected," "disconnected," or "unmanaged". 
Ubuntu Community Hub
Ubuntu Community Hub
 +3
3. Quick Software Fixes
If the hardware is detected but not connecting, try these steps:
Restart NetworkManager: This often fixes temporary software glitches.
bash
sudo systemctl restart NetworkManager
Use code with caution.

Manually Request an IP: Force your computer to ask the router for an address (replace enp0s3 with your actual device name found in step 2):
bash
sudo dhclient enp0s3
Use code with caution.

Check if Managed: If nmcli shows the device as "unmanaged," ensure managed=true is set in the NetworkManager configuration at /etc/NetworkManager/NetworkManager.conf. 
Ubuntu Community Hub
Ubuntu Community Hub
 +3
4. Advanced Troubleshooting
Driver Check: Run sudo lshw -C network. Look for the "configuration" line to see if a driver is listed (e.g., driver=e1000e). If it says "unclaimed," you are missing a driver.
Netplan Configuration: Modern Ubuntu (18.04+) uses Netplan. Check files in /etc/netplan/ to ensure the renderer is set to NetworkManager.
Dual-Boot Issues: If you also use Windows, disable Fast Startup in Windows settings, as it can sometimes "lock" the network card and prevent Linux from using it. 
Ask Ubuntu
Ask Ubuntu
 +4
AI responses may include mistakes. Learn more




No Ethernet/network - Support and Help - Ubuntu Discourse

Ubuntu Community Hub
Ubuntu not registering my Ethernet connection - Reddit

Reddit

Why is nmcli not configuring device? - Ask Ubuntu

Ask Ubuntu

Show all

undefined
undefined
undefined
10 sites
The output you are seeing is actually normal for a system that is currently using a spoofed or randomized MAC address. 
Unix & Linux Stack Exchange
Unix & Linux Stack Exchange
 +1
Here is the breakdown of what those two addresses mean:
link/ether 1a:f3:61:9a:d0:9b: This is your current (active) MAC address. This is the identifier your computer is currently showing to the router and the rest of the network.
permaddr 3c:7c:3f:bb:ae:c4: This is your permanent hardware MAC address. It is "burned into" your network card's hardware. 
Cisco Blogs
Cisco Blogs
 +2
Why do they look different?
Your system is currently overriding your real hardware address with a virtual one. This is common for two main reasons:
Privacy/Security: Modern Linux distributions often randomize MAC addresses by default to prevent tracking.
Manual Spoofing: You (or a script like macchanger) may have manually set a different MAC address. 
Ask Ubuntu
Ask Ubuntu
 +3
How to "Fix" It (Revert to Permanent)
If you want your computer to use its real hardware address (3c:7c:3f:bb:ae:c4), you can reset it via the terminal:
Bring the interface down:
sudo ip link set dev enp3s0 down
Reset the address:
sudo ip link set dev enp3s0 address 3c:7c:3f:bb:ae:c4
Bring the interface back up:
sudo ip link set dev enp3s0 up
To stop Ubuntu from randomising it in the future:
Check your NetworkManager configuration by looking at /etc/NetworkManager/NetworkManager.conf or files in /etc/NetworkManager/conf.d/. Ensure ethernet.cloned-mac-address is set to preserve or permanent instead of random. 
Unix & Linux Stack Exchange
Unix & Linux Stack Exchange
 +1
AI responses may include mistakes. Learn more




What's the difference between dev_addr and perm_addr of the struct ...

Stack Overflow
How to find unspoofed MAC address of NIC - Unix & Linux Stack Exchange

Unix & Linux Stack Exchange

spoofed mac address changes when trying to connect to network

Ask Ubuntu

Show all

undefined
undefined
undefined
13 sites
The dbx is the Forbidden Signature Database in Secure Boot. It acts as a "blacklist" of keys, signatures, and hashes belonging to vulnerable or malicious bootloaders and drivers that are no longer trusted to run. 
Microsoft Learn
Microsoft Learn
 +2
The specific figures you are seeing—size: 20688 (bytes) and keys: 430—represent a standard, modern "default factory" state because the list of revoked signatures has grown significantly over time due to security vulnerabilities. 
BINARLY
BINARLY
 +1
Why the DBX is so large
Historical Accumulation: Every time a vulnerability is found in a bootloader (like GRUB or Shim), its signature is added to the dbx to prevent attackers from using that old, broken version to bypass Secure Boot.
Default Factory Source: When you select "Restore Factory Keys" in BIOS, your motherboard manufacturer populates the dbx with the most recent list provided by Microsoft and UEFI.org.
Modern Standards: Older systems might have had only 20–70 keys. However, major security events like the BlackLotus or Baton Drop vulnerabilities forced Microsoft to release massive updates to the dbx. 
Reddit
Reddit
 +4
Key Components of your DBX status:
Size (20688): This is the actual memory footprint of the revoked signatures in the NVRAM.
Keys (430): This counts the number of individual entries (signatures or hashes) currently blocked by your firmware.
Key Source (Default): Confirms that these are the standard revocations provided by the vendor, rather than a custom list you enrolled yourself. 
Lenovo Documentation
Lenovo Documentation
 +2
Is it safe?
Yes. Having a large, populated dbx is actually better for security. A dbx that is empty or very small (e.g., 0KB) means your system is vulnerable to old exploits that could bypass Secure Boot entirely. Most modern systems will automatically update this list through Windows Update or Linux tools like fwupdmgr. 
Eclypsium
Eclypsium
 +4
AI responses may include mistakes. Learn more




From Trust to Trouble: The Supply Chain Implications of a Broken DBX

BINARLY

KB5012170: Security update for Secure Boot DBX - Microsoft Support

Microsoft Support
Firmware Security Realizations - Part 1 - Secure Boot and DBX - Eclypsium

Eclypsium

Show all

undefined
undefined
undefined
10 sites
To reboot Ubuntu (including version 23.10) into Recovery Mode, you must interrupt the standard boot process to access the GNU GRUB menu. 
Wiki Ubuntu (En)
Wiki Ubuntu (En)
 +1
Method 1: Accessing the Recovery Menu
Restart your computer.
Access the GRUB menu:
For BIOS systems: Press and hold the Shift key immediately after the manufacturer logo disappears.
For UEFI systems: Press the Esc key repeatedly (tapping it quickly) during the initial boot phase.
Navigate the menu:
Use the arrow keys to select Advanced options for Ubuntu and press Enter.
Select the entry that ends with (recovery mode) (typically the second one) and press Enter.
The Recovery Menu: Your system will boot into a limited menu with several tools:
resume: Boot normally.
clean: Free up disk space.
dpkg: Repair broken packages.
fsck: Check the file system for errors.
root: Drop to a root shell prompt for manual command entry. 
Ask Ubuntu
Ask Ubuntu
 +5
Method 2: Booting Directly to Root Shell
If you need immediate terminal access without the menu, select the root option from the Recovery Menu described above. 
Ask Ubuntu
Ask Ubuntu
 +1
Important: By default, the file system is mounted as read-only. To make changes (like resetting a password or editing files), run the following command once the prompt appears:
mount -o remount,rw / 
Ask Ubuntu
Ask Ubuntu
 +2
Troubleshooting Boot Entry
Menu Not Appearing: If you miss the timing and see the Ubuntu logo, you must restart and try the key (Shift or Esc) again.
Ubuntu Core: If you are using Ubuntu Core, you can reboot into recovery by holding the 1 key during boot or by running sudo snap reboot --recover from a terminal. 
Ask Ubuntu
Ask Ubuntu
 +3
AI responses may include mistakes. Learn more




Booting into recovery mode - Ubuntu Wiki

Wiki Ubuntu (En)
How do I boot into recovery mode? - Ask Ubuntu

Ask Ubuntu

How do I boot to recovery mode in Ubuntu 20.04? [duplicate]

Ask Ubuntu

Show all
If you’re seeing grub> or grub rescue> instead of a menu, it means GRUB can't find its configuration file (usually because the path changed or a partition moved).
Since you're on Ubuntu 23, here is how to find your files and boot manually from that prompt:
1. Find your Ubuntu partition
Type ls to see your drives/partitions (e.g., (hd0,gpt2)). Then, search each one for your boot folder:
bash
ls (hd0,gpt2)/boot/grub
Use code with caution.

Keep trying partitions until you see a list of files like grub.cfg.
2. Set the paths
Once you find the right partition (we'll use (hd0,gpt2) as an example), tell GRUB where to look:
bash
set root=(hd0,gpt2)
set prefix=(hd0,gpt2)/boot/grub
Use code with caution.

3. Load the menu
Try to trigger the normal menu you were looking for:
bash
insmod normal
normal
Use code with caution.

If the menu appears: Great! Select "Advanced Options" -> "Recovery Mode."
If it fails: You’ll need to load the kernel manually (see below).
4. Manual Boot (If "normal" didn't work)
You need to point GRUB to the kernel (vmlinuz) and the ramdisk (initrd).
Load the kernel:
linux /boot/vmlinuz-root=UUID=xxxx ro recovery nomodeset
(Tip: Use Tab after typing /boot/vmlinuz to auto-complete the version number).
Load the initrd:
initrd /boot/initrd.img-xxx (Use Tab again).
Boot:
boot
Why did this happen?
If you recently messed with Secure Boot or those dbx keys you mentioned, the signature on the GRUB bootloader might have been revoked (blacklisted). If you get "Access Denied" or "Signature Verification Failed" at this prompt, you may need to temporarily Disable Secure Boot in your BIOS to get back into the OS and repair the bootloader.
AI responses may include mistakes. Learn more




If you are trying to disable SSH access or the SSH daemon manually from a recovery shell or GRUB, you have a few ways to do it depending on what exactly you want to "remove."
1. Disable the SSH Service (Systemd)
The cleanest way to prevent SSH from starting on boot is to mask the service. From your root shell:
bash
systemctl mask ssh
Use code with caution.

Why mask? Unlike disable, masking creates a symbolic link to /dev/null, making it impossible for the service to start even if another process tries to trigger it.
2. Manual Config: Edit sshd_config
If you want to keep the service running but block all connections, edit the config file:
bash
nano /etc/ssh/sshd_config
Use code with caution.

Find or add these lines to set them to no:
Port 0 (effectively disables listening, though usually you just stop the service).
PubkeyAuthentication no
PasswordAuthentication no
PermitRootLogin no
3. "The Nuclear Option" (Removing the binary)
If you are in a recovery environment and want to ensure SSH cannot run at all:
bash
chmod 000 /usr/sbin/sshd
Use code with caution.

This removes all execution permissions from the SSH daemon. To undo it later: chmod 755 /usr/sbin/sshd.
4. Remove SSH from Kernel Boot Parameters
If you manually added an SSH hook to your GRUB line (sometimes used in remote LUKS unlocking or cloud-init):
Edit /etc/default/grub.
Look for the line GRUB_CMDLINE_LINUX_DEFAULT.
Remove any parameters like ip=dhcp or ssh=1 if they exist.
Run update-grub.
Quick Tip: If you just want to kill a currently running session while in recovery, use pkill -9 sshd.
AI responses may include mistakes. Learn more




If the malware removed /usr/bin/sshd (or /usr/sbin/sshd), the service is effectively dead because the binary it tries to execute is gone. To clean up the "ghost" configuration and ensure it doesn't try to start again, you should focus on purging the package and checking for persistence hooks.
Since you are in a recovery environment, follow these steps:
1. Purge the Package
Even if the file is gone, the system still thinks the service exists. Remove all configuration files and service units:
bash
apt-get purge openssh-server
Use code with caution.

Note: If you have no internet in recovery, this will remove the metadata, but you won't be able to "reinstall" until you have a connection.
2. Check for "No-Reinstall" persistence
Malware often leaves "immutable" flags or scripts to prevent you from fixing the system. Check these three spots:
systemd override: Check /etc/systemd/system/ssh.service.d/. If there's a file there, delete it.
init.d scripts: Check /etc/init.d/ssh. Delete it if purge didn't.
Attributes: If you try to delete a file and it says "Operation not permitted" (even as root), run:
chattr -i /etc/ssh/sshd_config (removes the "immutable" flag).
3. Check for the "Fake" SSH
Check if the malware replaced the sshd binary with a script or a different malicious file:
bash
ls -la /usr/sbin/sshd
ls -la /usr/bin/sshd
Use code with caution.

If either file exists but has a weird size or date, delete them manually: rm -f /usr/sbin/sshd.
4. Verify the Port is Closed
To make sure nothing else is listening on the standard SSH port (22):
bash
ss -tulpn | grep :22
Use code with caution.

If something is there, note the PID and kill it: kill -9 <PID>.
5. Check Cron/Systemd Timers
Malware often uses a "watcher" to re-download or re-enable SSH.
Check /etc/crontab and /etc/cron.d/ for weird lines.
Check systemctl list-timers for any suspicious tasks scheduled to run soon.
If you need SSH back later: Once the system is clean and you have internet, run sudo apt-get install --reinstall openssh-server.
AI responses may include mistakes. Learn more





undefined
undefined
undefined
7 sites
In a standard Ubuntu installation, /usr/sbin/sssd (System Security Services Daemon) is a legitimate system file used to manage authentication against network sources like Active Directory or LDAP. 
Ubuntu
Ubuntu
 +1
However, the specific details you've shared—a 2025 date and its presence on a new install where you've already found malware—require immediate attention.
1. The Date Issue (Aug 2025)
Normal Packages: Usually, system files have "old" dates (2023 or 2024 for Ubuntu 23.10) because they reflect when the package was originally built by developers, not when you installed it.
Future Dates: A file dated August 2025 is highly suspicious unless your system clock is currently set to the future. Malware often "timestomps" files (sets them to a future or far-past date) to hide from security scanners that look for recently modified files. 
Ask Ubuntu
Ask Ubuntu
2. Is it a Fake Binary?
Because you mentioned malware already removed your real sshd, it is possible this sssd is a replacement binary meant to look like a legitimate system service.
Legitimate sssd: Owned by root:root, typically with 755 permissions.
Check the file type: Run this command to see what it actually is:
bash
file /usr/sbin/sssd
Use code with caution.

If it says "shell script" instead of "ELF 64-bit executable," it is almost certainly malicious. 
3. Verify Against the Package Database
You can check if the file matches what Ubuntu officially installed. This is the most reliable way to tell if it has been tampered with:
bash
dpkg -S /usr/sbin/sssd
Use code with caution.

If it says "no path found": The file was manually placed there by a user or script and is malicious.
If it belongs to a package (e.g., sssd-common): Verify its integrity:
bash
debsums -s sssd-common
Use code with caution.

If debsums reports a mismatch or isn't installed, the file may have been replaced.
4. Recommended Action
Since you have already confirmed a malware infection that was deep enough to delete system binaries (sshd):
Do not trust this install. Malware that modifies system binaries and uses future-dated files in /usr/sbin/ is difficult to fully clean.
Wipe and Reinstall: The only way to be 100% sure your system is safe is to back up your personal data (not system files), wipe the drive, and perform a clean install.
Check BIOS/UEFI: Since you were asking about Secure Boot dbx keys earlier, ensure your BIOS is set to "User Mode" or "Standard" with factory keys to prevent the malware from surviving in the bootloader. 
Ask Ubuntu
Ask Ubuntu
 +1
AI responses may include mistakes. Learn more




What are the default permissions for the /sbin/ directory? - Ask Ubuntu

Ask Ubuntu
Can my Ubuntu 22.04 LTS be compromised?

Ask Ubuntu

Introduction to network user authentication with SSSD - Ubuntu

Ubuntu
Show all
Help is available
Speak with someone today
Shout
Hours: 24/7
Text SHOUT to 85258
Official website
Learn more


Because Firefox is installed as a Snap in Ubuntu 23, its files are located in /snap/firefox.
However, /snap is a read-only compressed filesystem. You cannot delete folders inside /snap/ using rm -rf because they are technically "mounted" like a virtual disk.
To remove it completely when purge fails, follow these steps:
1. Unmount the "Ghost" Snap
If the folder is still there after a purge, it’s likely because the mount point is stuck. Find the mount name:
bash
mount | grep firefox
Use code with caution.

If you see entries, unmount them manually (replace XXXX with the specific number/version shown in the path):
bash
sudo umount /snap/firefox/XXXX
Use code with caution.

2. Remove the Snap Metadata
If snap remove --purge firefox didn't work, try cleaning the core snap directory:
bash
sudo rm -rf /var/lib/snapd/snaps/firefox_*.snap
Use code with caution.

3. Delete the User Data Folders
The actual "clutter" (cache, history, malware-infected profiles) is kept in your home directory and the system common directory. Delete these:
bash
# System-wide snap data
sudo rm -rf /var/snap/firefox

# Your personal user data
rm -rf ~/snap/firefox
Use code with caution.

4. Clean the Mount Points
If the empty /snap/firefox folder still exists after unmounting:
bash
sudo rmdir /snap/firefox
Use code with caution.

Important Security Note
Since you mentioned malware earlier:
Malware often hides in ~/snap/firefox/common/.mozilla.
If you see a folder in /snap/ that refuses to delete even after umount, check if your system has been compromised with a Rootkit. A rootkit can "hook" the filesystem to make its folders invisible or undeletable to standard commands.
Run lsattr /snap/firefox (if possible). If you see an i (immutable) flag, run sudo chattr -i /snap/firefox before trying to delete it again.
AI responses may include mistakes. Learn more





undefined
undefined
undefined
17 sites
To fix a "web error" in rkhunter (Rootkit Hunter) while updating its database, you usually need to change the mirror or the command it uses to connect to the internet. 
neilzone.co.uk
neilzone.co.uk
 +2
1. Fix rkhunter Update Error
If you are seeing a web error during rkhunter --update, it is often because it is trying to use http instead of https, or it is blocked by a security setting. 
neilzone.co.uk
neilzone.co.uk
 +1
Open the configuration file:
sudo nano /etc/rkhunter.conf
Allow Mirror Updates: Find these lines and ensure they are set to 1 and 0 as shown:
UPDATE_MIRRORS=1
MIRRORS_MODE=0 (This allows it to use any mirror, not just local ones)
Change the Web Command: If it still fails, find the WEB_CMD line. If it is set to "/bin/false", it is purposely blocked. Change it to use wget or empty quotes to let it auto-select:
WEB_CMD=""
Save and exit (Ctrl+O, Enter, Ctrl+X). 
neilzone.co.uk
neilzone.co.uk
 +3
2. Change Ubuntu Software Mirrors (Apt) 
If you meant you need to change your general Ubuntu software mirrors because apt update is failing: 
Manual Terminal Method:
Open your sources list: sudo nano /etc/apt/sources.list.
Use Ctrl+\ (the replace shortcut in nano) to replace your regional mirror (e.g., us.archive.ubuntu.com) with the main global mirror: archive.ubuntu.com.
Save and run sudo apt update.
Automatic Command-Line Method:
If you can still install packages, use apt-mirror-updater to find the fastest mirror for you automatically:
bash
sudo pip3 install apt-mirror-updater
sudo apt-mirror-updater --auto-change-mirror
Use code with caution.

 
Ask Ubuntu
Ask Ubuntu
 +6
3. Ensure Networking is Active 
If you are in Recovery Mode, mirrors will not work until you enable networking: 
www.raiddatarecoveryspecialists.co.uk
www.raiddatarecoveryspecialists.co.uk
 +1
In the Recovery Menu, select network first to enable the internet.
Then drop to the root shell to make the configuration changes mentioned above.
Remount the drive so you can save your changes: mount -o remount,rw /. 
Wiki Ubuntu (En)
Wiki Ubuntu (En)
 +2
AI responses may include mistakes. Learn more




Fixing rkhunter's 'Update failed' error - Neil's blog

neilzone.co.uk
Invalid WEB_CMD configuration option: Relative pathname: "/bin/false"

Unix & Linux Stack Exchange

Detect Linux Security Holes and Rootkits with Rkhunter on Ubuntu - Atlantic.Net

Atlantic.Net

Show all
If you are seeing INSTALLDI=/usr (or similar) at the very top of a configuration file (like rkhunter.conf), it means the software is hardcoded to look for its binaries and libraries in the /usr directory.
Since you mentioned malware and a suspicious sssd binary in /usr/sbin, having your security tools (like rkhunter) pointed at /usr is exactly what you want—but only if you can trust the tools themselves.
How to handle this in rkhunter.conf:
Verify the Path: Ensure the line actually points to where the binaries live. Usually, it looks like:
INSTALLDIR=/usr
Check for "Whiteouts": Malware often adds its own "safe" files to the rkhunter whitelist so they aren't flagged. Scroll down or search (Ctrl+W in nano) for:
RTKT_FILE_WHITELIST
RTKT_DIR_WHITELIST
If you see /usr/sbin/sssd or your weird aug 2025 files whitelisted here, delete those lines.
If you are trying to "Fix" the install:
If rkhunter itself was installed by the malware, it might be a "fake" version designed to tell you the system is clean.
Run a check against the package: dpkg -V rkhunter
If it shows errors (especially on binaries): The scanner itself is compromised.
The "Mirror" error you mentioned earlier:
If you are still getting a web/mirror error while INSTALLDIR=/usr is set:
Check the UPDATE_MIRRORS setting again.
Make sure your DNS is working: ping -c 3 google.com. If ping fails, the "web error" is a network issue, not a config issue.
If you're in a recovery shell, you must run dhclient to get an IP address before the mirrors will connect.
AI responses may include mistakes. Learn more




If you want to go "brutal" on a suspected rootkit from the console, you need to stop playing by the OS's rules, because a rootkit hides by lying to standard commands like ls and ps.
Here is the "scorched earth" search combo to run from your root shell:
1. The "Deep Diver" (Find Hidden/Future Files)
This searches for anything modified in the "future" (like your Aug 2025 file) or files with hidden attributes that prevent deletion:
bash
# Find future-dated files (adjust year if needed)
find / -newermt "2025-01-01" -ls 2>/dev/null

# Find files with the "Immutable" or "Append-only" flag (common rootkit trick)
lsattr -R / 2>/dev/null | grep -E "\-i\-|\-a\-"
Use code with caution.

2. The "Binary Audit" (Check for Lying Binaries)
Since your sshd vanished, your other binaries might be fakes. This tells dpkg to verify every single file against the official Ubuntu hash. If it says 5 (MD5 hash) or S (Size), the file is compromised:
bash
dpkg -V | grep -E '^.{8} '
Use code with caution.

3. The "Network Sniffer" (See the Secret Call-outs)
Rootkits often hide their connections from netstat. Use ss with the "all/numeric" flags to bypass name resolution:
bash
ss -antp | grep -v "127.0.0.1"
Use code with caution.

4. The RKHunter "Propcheck" (Double Mode)
Since you've got rkhunter configured, run it in interactive/brutal mode. This will force it to check for hidden dirs and string-check every binary:
bash
sudo rkhunter --check --enable all --disable none --sk
Use code with caution.

5. Check for "Procless" Processes
If a process shows up in /proc/ but not in ps, it’s a rootkit.
bash
unhide-posix quick
unhide-posix reverse
Use code with caution.

(If unhide isn't installed, you're stuck with manual ls /proc vs ps aux comparison).
The "Brutal" Reality:
If dpkg -V starts spitting out a long list of modified files in /usr/bin or /lib, stop. The "savaging" has already happened. At that point, the rootkit owns the kernel, and no command you type can be trusted. You’ll need to boot from a Live USB and wipe the drive from the outside.
AI responses may include mistakes. Learn more




if it’s not in /usr, the malware likely moved the rkhunter installation to a "hidden" or "unusual" directory to bypass your security checks.
To find where it’s actually hiding, run this brutal search for the binary and its config:
1. Locate the Hidden Install
Search the entire drive for any file named rkhunter or the main config file:
bash
find / -name "rkhunter*" 2>/dev/null
find / -name ".rkhunter*" 2>/dev/null
Use code with caution.

Look for paths like /dev/shm, /tmp, /var/tmp, or hidden folders like /etc/.rk.
2. Check for "Alias" Trickery
If you type rkhunter and it runs, but it's not in /usr, it might be an alias or a redirected path. Check where the shell thinks it is:
bash
type -a rkhunter
which rkhunter
Use code with caution.

3. The "Rootkit Savaging" Command (Hardcore Mode)
If you want to find everything that shouldn't be there, run this "Double Mode" scan. It ignores the standard file system and looks for deleted files still running in memory and hidden directories:
bash
# 1. Find processes running from deleted files (Classic Rootkit)
ls -alR /proc/*/exe 2>/dev/null | grep "deleted"

# 2. Find hidden directories with weird names (e.g., "...")
find / -type d -name ".*" 2>/dev/null

# 3. Search for the "Aug 2025" timestamp everywhere
find / -newermt "2025-01-01" ! -path "/proc/*" ! -path "/sys/*" -ls 2>/dev/null
Use code with caution.

4. Force rkhunter to use a specific Config
If you found the config file in a weird spot (e.g., /tmp/.hidden/rkhunter.conf), force the scan to use it:
bash
/path/to/rkhunter --configfile /path/to/weird/rkhunter.conf --check --sk
Use code with caution.

Warning: If the malware moved the "official" security tools, it likely patched them to ignore its own files. If dpkg -S rkhunter says the package isn't installed but the command works, you are running a fake scanner. Stop using it and boot from a Live USB.
AI responses may include mistakes. Learn more




It looks like your rkhunter installation is actually exactly where it should be (in /usr/bin/rkhunter and /etc/rkhunter.conf). If it's telling you it’s "not in usr," the malware might be using a path redirection or a symbolic link to trick the scanner into looking at a fake directory.
Here is your brutal, double-mode, "savaging" search command to run right now. This bypasses the normal OS "lies" by looking for hidden attributes, future dates, and hijacked binaries.
1. The "Brutal Audit" (The All-in-One Command)
Copy and paste this (as root) to find everything "impossible":
bash
# Finds future files (2025), hidden "dot" files in system folders, 
# and files with the 'immutable' flag that can't be deleted.
find / -newermt "2025-01-01" -ls 2>/dev/null; \
find /usr /bin /sbin /lib -name ".*" -ls 2>/dev/null; \
lsattr -R / 2>/dev/null | grep -E "\-i\-|\-a\-"
Use code with caution.

2. Force rkhunter into "Paranoid" Mode
Since rkhunter is in /usr/bin/rkhunter, run it with these specific flags to force it to ignore its own whitelist and check for hidden strings:
bash
sudo rkhunter --check --enable all --disable none --rwo --sk
Use code with caution.

--rwo: (Report Warnings Only) - Cuts the noise so you only see the hits.
--sk: (Skip Keypress) - Lets it run full speed.
3. Check for the "Invisible" Process
If the malware is smart, it's hiding its process from ps. Run this to find "ghost" processes:
bash
ls /proc | grep -vE '^[0-9]+$' | xargs -I{} echo "Suspicious proc entry: {}"
# AND check for deleted-but-running files
ls -alR /proc/*/exe 2>/dev/null | grep "deleted"
Use code with caution.

4. Fix the "Mirror" Error Manually
If rkhunter still gives a "web error," it's because the malware likely blocked the update URL in your hosts file. Check it:
bash
cat /etc/hosts
Use code with caution.

If you see rkhunter.sourceforge.net or mirror pointed to 127.0.0.1, delete those lines.
The "Aug 2025" file is your smoking gun. If find shows that date on any file in /bin or /sbin, that file is a malicious backdoor. Delete it immediately with rm -f, and if it refuses, run chattr -i [filename] first.
AI responses may include mistakes. Learn more





undefined
undefined
undefined
7 sites
If rkhunter refuses to run and claims /usr is the main configuration directory, it usually means the INSTALLDIR variable in your configuration is missing or pointing to the wrong place. 
SourceForge
SourceForge
 +1
In Ubuntu, the installation expects to find its data and scripts in specific system paths. If this is broken, follow these steps to force it to run:
1. Fix the INSTALLDIR in Config 
Open your configuration file and ensure the installation directory is explicitly set to /usr: 
bash
sudo nano /etc/rkhunter.conf
Use code with caution.

Find the line for INSTALLDIR (or add it near the top) and set it exactly like this:
INSTALLDIR=/usr 
Ynet Interactive
Ynet Interactive
Note: Also verify these paths are correct in the same file: 
DBDIR=/var/lib/rkhunter/db
SCRIPTDIR=/usr/share/rkhunter/scripts (or /usr/lib/rkhunter/scripts) 
Ynet Interactive
Ynet Interactive
2. Verify and Reinstall (If Config is corrupted)
If the malware tampered with the configuration so badly that it won't start, the fastest way to get a clean, working version is to purge and reinstall:
bash
sudo apt-get purge rkhunter
sudo apt-get install rkhunter
Use code with caution.

This restores the default Ubuntu configuration and paths. 
OneUptime
OneUptime
 +1
3. The "Brutal" Savaging Scan Command
Once you've fixed the config error, use this "double-mode" command for the most aggressive search possible:
bash
sudo rkhunter --check --enable all --disable none --rwo --sk
Use code with caution.

--rwo: Report Warnings Only (hides all the "OK" messages so you only see the hits).
--sk: Skip Keypress (runs the whole scan without stopping). 
DEV Community
DEV Community
 +1
4. If it still won't run: "The Manual Savager"
If rkhunter is completely blocked, use this manual command to find that August 2025 malware and any other hidden threats immediately:
bash
# Finds any file modified in the future (malware timestamping)
find / -newermt "2025-01-01" -ls 2>/dev/null

# Find hidden directories in sensitive system areas
find /usr /bin /sbin /lib -name ".*" -ls 2>/dev/null

# Check for processes running from deleted files (common for rootkits)
ls -alR /proc/*/exe 2>/dev/null | grep "deleted"
Use code with caution.

Critical Warning: If you are seeing INSTALLDI=/usr (misspelled without the 'R') as you mentioned earlier, that is a common sign of malicious tampering with your security tools. Reinstall the package immediately to overwrite the fake config. 
SourceForge
SourceForge
AI responses may include mistakes. Learn more




Thread: [Rkhunter-users] Intallation Error (INSTALLDIR) | Rootkit Hunter

SourceForge
Important Tips To Make Your cPanel & WHM Based Server More ...

Ynet Interactive

How to Configure Rootkit Detection with rkhunter on Ubuntu - OneUptime

OneUptime

Show all
In Ubuntu, those specific warnings for adduser, egrep, fgrep, and ldd are actually false positives. On modern Ubuntu systems, those commands are natively scripts, not binary files, so rkhunter flags them because it expects binaries.
However, given your earlier "Aug 2025" discovery and the deleted sshd, you need to verify if these scripts have been tampered with.
1. The "Brutal" Verification (Hash Check)
Run this to see if the system's official record matches what is on your disk. If it returns nothing, the files are safe. If it returns a line starting with 5 or S, they are compromised:
bash
dpkg -V base-files grep libc-bin adduser
Use code with caution.

2. The "Savaging" Search (Double Mode)
Since you're at the prompt, run this "brutal" combo to find the real malware hiding behind those scripts:
bash
# Find any file modified in the future (Aug 2025)
find / -newermt "2025-01-01" -ls 2>/dev/null

# Find hidden files in the binary folders
ls -la /usr/bin/.* /usr/sbin/.* /bin/.* /sbin/.* 2>/dev/null

# See what is REALLY listening on your network
ss -tulpn
Use code with caution.

3. Check for the "Fake" SSSD
You mentioned /usr/sbin/sssd from Aug 2025 earlier. Check it specifically:
bash
file /usr/sbin/sssd
ls -l /usr/sbin/sssd
Use code with caution.

If that file still exists with a 2025 date, delete it immediately:
bash
sudo chattr -i /usr/sbin/sssd
sudo rm -f /usr/sbin/sssd
Use code with caution.

Why the ^C?
It looks like you’re trying to break out of a loop or a frozen terminal. If the system is lagging or unresponsive, the malware might be pinning your CPU to hide itself. Use top or htop to see what's eating your resources.
Bottom line: If dpkg -V shows that your core binaries are modified, stop cleaning. The system is "savaged" beyond reliable repair. Back up your files and wipe the drive.
AI responses may include mistakes. Learn more




