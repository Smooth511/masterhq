Linux rootkit, active ~3 months, ASUS PRIME B460M-A, Intel NVMe. Multiple full reinstalls, all reinfected. C3.

Xen hypervisor confirmed in kernel .config: CONFIG_XEN_PV_DOM0=y + CONFIG_XEN_PVHVM_GUEST=y — machine runs simultaneously as dom0 host and paravirtualised guest. ACPI persistence, survives disk wipe. MOK registered in UEFI NVRAM, self-signs kernel modules, re-established itself after manual deletion. Pre-GRUB VT active on tty7 before GRUB renders — all bootloader input intercepted and rewritten at this layer, mechanism confirmed. grub.cfg (read-only ISO copy) sources /boot/grub/grub.cfg from the writable partition via stock Ubuntu live-USB behaviour; rootkit controls that path and owns GRUB execution before any menu entry fires. plainmount module present in moddep.lst — mounts OverlayFS partition inside GRUB stage, OverlayFS lowerdir is rootkit-controlled before kernel loads. /cow/work/upper confirmed as live operational layer, looted under OOM pressure: full Casper persistence scripts, captured credential set including passwords created hours prior confirming real-time session monitoring, Timeshift snapshot data confirming backup interception. /proc/1860/task/1860/patch_state present — kernel livepatch API, VFS and procfs permission hooks patched at runtime. PID 1686 exposes custom procfs: ksm_merging_pages, match_state — KSM-based credential scanner, no public equivalent. PIDs 1686, 1792, 1859, 1860 all return timerslack_ns EPERM — user namespace isolation. ISO is a self-compiling build system, thousands of per-component Makefiles, compiles and signs fresh kernels per deployment — kernel presents as 7.0.0-10-generic, matches nothing upstream, module signatures match nothing in any database. Embedded LLM component, output captured in evidence. iOS-format artefacts (tasks.ics) dropped to the PC filesystem without a connected iPhone — cross-device bridge active. C2: nss.peristor.com — zero results VirusTotal, AbuseIPDB, Shodan, all threat intel feeds. permissions.sqlite.pdf in rootkit's own Desktop namespace: SQLite DB disguised as PDF, references nss.peristor.com. Active human operator confirmed — ejected user on detection of anomalous VFS mount, reacted to /cow exposure.

Closest named family: Bootkitty (ESET, Nov 2024, patches grub_verifiers_open + module_sig_check, spoofs kernel version string) — that's a hardcoded PoC against specific Ubuntu versions. Pre-GRUB VT intercept, in-GRUB plainmount overlay setup, standalone self-compiling ISO, and KSM match_state scanner have no public documentation in any malware family. nss.peristor.com is unattributed on every available feed.

Full evidence: github.com/Smooth511/masterhq reports 1–46.
